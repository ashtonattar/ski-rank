# ski-rank (Slope Battles)

Before working on this app, read `PROJECT_STATUS.md` in this directory — it tracks the app's architecture, features, and current progress/focus.

After making a meaningful change to the app (new feature, notable fix, architecture shift), update `PROJECT_STATUS.md`:
- Keep "Recent Work" current (newest first); prune older entries into a brief summary once the list gets long.
- Update "Current Focus" if the overall focus area has shifted.
- Bump the "Last updated" date at the bottom.
